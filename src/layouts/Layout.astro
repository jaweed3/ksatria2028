---
const { title = "KSATRIA 2028 — Kongres Satu Abad Sumpah Pemuda Republik Indonesia" } = Astro.props;
const description = "KSATRIA 2028 adalah forum nasional pemuda Indonesia memperingati 100 tahun Sumpah Pemuda. 28 Oktober 2028, Gedung MPR RI & GBK Jakarta. 1928: Melahirkan Indonesia — 2028: Menentukan Indonesia.";
---
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{title}</title>
  <meta name="description" content={description}>
  <meta name="keywords" content="KSATRIA 2028, Sumpah Pemuda, Kongres Pemuda, Deklarasi Pemuda Indonesia, Indonesia Emas 2045">
  <meta name="author" content="Panitia Nasional KSATRIA 2028">
  <meta property="og:type" content="website">
  <meta property="og:title" content="KSATRIA 2028 — Kongres Satu Abad Sumpah Pemuda Republik Indonesia">
  <meta property="og:description" content="1928: Melahirkan Indonesia — 2028: Menentukan Indonesia. 28 Oktober 2028, Jakarta.">
  <meta property="og:site_name" content="KSATRIA 2028">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;0,700;0,900;1,400;1,700&family=Crimson+Pro:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Space+Mono:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
  <style is:global>
    /* ============================================================
       DESIGN SYSTEM — KSATRIA 2028
       ============================================================ */
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }

    :root {
      /* Primary Palette */
      --color-crimson:        #8B1A1A;
      --color-crimson-dark:   #5C0F0F;
      --color-crimson-light:  #B02020;
      --color-gold:           #C9A84C;
      --color-gold-light:     #E8C96A;
      --color-gold-dark:      #9A7A30;
      --color-ivory:          #F5F0E8;
      --color-ivory-dark:     #EDE5D5;
      --color-paper:          #FAF7F2;
      --color-deep-black:     #0D0D0D;
      --color-charcoal:       #1A1A1A;
      --color-ash:            #2C2C2C;
      --color-text-primary:   #1A0A0A;
      --color-text-secondary: #4A3030;
      --color-text-muted:     #7A6060;
      --color-text-on-dark:   #F5F0E8;
      --color-border:         rgba(139, 26, 26, 0.15);
      --color-border-gold:    rgba(201, 168, 76, 0.3);

      /* Typography */
      --font-display: 'Playfair Display', 'Times New Roman', Georgia, serif;
      --font-body:    'Crimson Pro', 'Georgia', Times, serif;
      --font-mono:    'Space Mono', 'Courier New', Courier, monospace;

      /* Spacing */
      --section-pad-y: clamp(4rem, 8vw, 8rem);
      --section-pad-x: clamp(1.5rem, 5vw, 4rem);
      --container: 1280px;
      --container-narrow: 840px;

      /* Animation */
      --dur-instant: 80ms; --dur-fast: 200ms; --dur-normal: 400ms;
      --dur-slow: 700ms; --dur-deliberate: 1000ms; --dur-grand: 1400ms;
      --ease-out: cubic-bezier(0.0, 0.0, 0.2, 1);
      --ease-power: cubic-bezier(0.16, 1, 0.3, 1);
      --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
      --ease-snap: cubic-bezier(0.87, 0, 0.13, 1);
    }

    body {
      font-family: var(--font-body);
      color: var(--color-text-primary);
      background: var(--color-ivory);
      line-height: 1.7;
      font-size: 1.0625rem;
      -webkit-font-smoothing: antialiased;
    }

    /* Scroll Progress */
    .scroll-progress {
      position: fixed; top: 0; left: 0; width: 0%; height: 3px;
      background: linear-gradient(90deg, var(--color-crimson), var(--color-gold));
      z-index: 9999; transition: width 100ms linear;
    }

    /* ============================================================
       NAVBAR
       ============================================================ */
    .navbar {
      position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
      height: 70px; display: flex; align-items: center;
      padding: 0 var(--section-pad-x);
      transition: all var(--dur-normal) var(--ease-out);
      animation: navbarSlideDown var(--dur-slow) var(--ease-power) 100ms both;
    }
    .navbar.scrolled {
      background: rgba(13, 13, 13, 0.95);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(201, 168, 76, 0.2);
    }
    .navbar-inner {
      width: 100%; max-width: var(--container); margin: 0 auto;
      display: flex; align-items: center; justify-content: space-between;
    }
    .nav-logo {
      font-family: var(--font-mono); font-size: 0.875rem;
      color: var(--color-gold); text-decoration: none; letter-spacing: 0.15em;
      font-weight: 700;
    }
    .nav-links { display: flex; align-items: center; gap: 2rem; list-style: none; }
    .nav-links a {
      font-family: var(--font-mono); font-size: 0.7rem; letter-spacing: 0.12em;
      text-transform: uppercase; color: var(--color-text-on-dark);
      text-decoration: none; opacity: 0.8; transition: opacity var(--dur-fast);
      position: relative;
    }
    .nav-links a::after {
      content: ''; position: absolute; bottom: -2px; left: 0; width: 0; height: 1px;
      background: var(--color-gold); transition: width var(--dur-normal) var(--ease-out);
    }
    .nav-links a:hover { opacity: 1; }
    .nav-links a:hover::after { width: 100%; }
    .nav-cta {
      background: var(--color-crimson); color: var(--color-ivory);
      font-family: var(--font-mono); font-size: 0.7rem; letter-spacing: 0.1em;
      text-transform: uppercase; padding: 0.6rem 1.25rem; border: none;
      text-decoration: none; transition: all var(--dur-fast);
    }
    .nav-cta:hover { background: var(--color-crimson-dark); color: var(--color-gold); }
    .nav-toggle { display: none; background: none; border: none; color: var(--color-gold); font-size: 1.5rem; cursor: pointer; }

    @media (max-width: 900px) {
      .nav-links { display: none; }
      .nav-links.open {
        display: flex; flex-direction: column; position: fixed; inset: 0;
        background: rgba(13, 13, 13, 0.98); backdrop-filter: blur(20px);
        justify-content: center; align-items: center; gap: 2.5rem;
        z-index: 999; animation: navOverlayOpen var(--dur-normal) var(--ease-out);
      }
      .nav-links a { font-size: 1rem; }
      .nav-toggle { display: block; z-index: 1001; }
    }

    /* ============================================================
       SECTION BASE
       ============================================================ */
    .section { padding: var(--section-pad-y) var(--section-pad-x); }
    .section-inner { max-width: var(--container); margin: 0 auto; }
    .section-inner--narrow { max-width: var(--container-narrow); margin: 0 auto; }

    .section-label {
      font-family: var(--font-mono); font-size: 0.75rem; letter-spacing: 0.2em;
      text-transform: uppercase; text-align: center; margin-bottom: 1.5rem;
    }
    .section-title {
      font-family: var(--font-display); font-size: clamp(2rem, 4.5vw, 3.5rem);
      font-weight: 700; text-align: center; line-height: 1.15; margin-bottom: 1rem;
    }
    .section-divider {
      width: 60px; height: 2px; margin: 1.5rem auto 2rem;
    }
    .section-lead {
      font-size: 1.125rem; text-align: center; max-width: 640px;
      margin: 0 auto 3rem; line-height: 1.8;
    }

    /* ============================================================
       SCROLL ANIMATIONS
       ============================================================ */
    [data-animate] {
      opacity: 0;
      transition: opacity var(--dur-slow) var(--ease-power),
                  transform var(--dur-slow) var(--ease-power);
    }
    [data-animate="fade-up"] { transform: translateY(40px); }
    [data-animate="slide-left"] { transform: translateX(-50px); }
    [data-animate="slide-right"] { transform: translateX(50px); }
    [data-animate="scale-up"] { transform: scale(0.9); }
    [data-animate="fade-in"] { transform: none; }
    [data-animate].is-visible { opacity: 1; transform: none !important; }

    /* ============================================================
       KEYFRAMES
       ============================================================ */
    @keyframes navbarSlideDown {
      from { transform: translateY(-100%); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
    @keyframes navOverlayOpen {
      from { opacity: 0; transform: scale(0.96); }
      to { opacity: 1; transform: scale(1); }
    }
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(40px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes fadeDown {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    @keyframes heroWordmark {
      0% { opacity: 0; transform: translateY(60px) scale(0.95); letter-spacing: 0.3em; }
      100% { opacity: 1; transform: translateY(0) scale(1); letter-spacing: normal; }
    }
    @keyframes drawLine {
      from { width: 0; opacity: 0; }
      to { width: 60px; opacity: 1; }
    }
    @keyframes taglineReveal {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes scaleUp {
      from { opacity: 0; transform: scale(0.88); }
      to { opacity: 1; transform: scale(1); }
    }
    @keyframes scrollBounce {
      0%, 100% { transform: translateY(0); opacity: 1; }
      50% { transform: translateY(8px); opacity: 0.6; }
    }
    @keyframes shimmer {
      0% { background-position: -200% center; }
      100% { background-position: 200% center; }
    }
    @keyframes glowPulse {
      0%, 100% { box-shadow: 0 0 0 0 rgba(201, 168, 76, 0); }
      50% { box-shadow: 0 0 20px 4px rgba(201, 168, 76, 0.3); }
    }
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-12px); }
    }

    /* ============================================================
       REDUCED MOTION
       ============================================================ */
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
        scroll-behavior: auto !important;
      }
      [data-animate] { opacity: 1 !important; transform: none !important; }
    }

    /* ============================================================
       SECTION COLOR VARIATIONS
       ============================================================ */
    .section-tentang { background: var(--color-ivory); }
    .section-nilai { background: var(--color-crimson); position: relative; overflow: hidden; }
    .section-nilai::before {
      content: ''; position: absolute; inset: 0;
      background-image:
        repeating-linear-gradient(45deg, transparent, transparent 10px, rgba(255,255,255,0.02) 10px, rgba(255,255,255,0.02) 11px),
        repeating-linear-gradient(-45deg, transparent, transparent 10px, rgba(255,255,255,0.02) 10px, rgba(255,255,255,0.02) 11px);
      pointer-events: none;
    }
    .section-kegiatan { background: var(--color-ivory-dark); }
    .section-komisi { background: var(--color-deep-black); position: relative; overflow: hidden; }
    .section-komisi::after {
      content: ''; position: absolute; inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
      pointer-events: none; mix-blend-mode: overlay; z-index: 1;
    }
    .section-output { background: var(--color-ivory); }
    .section-representasi { background: var(--color-crimson); position: relative; overflow: hidden; }
    .section-representasi::before {
      content: ''; position: absolute; inset: 0;
      background-image:
        repeating-linear-gradient(45deg, transparent, transparent 10px, rgba(255,255,255,0.02) 10px, rgba(255,255,255,0.02) 11px),
        repeating-linear-gradient(-45deg, transparent, transparent 10px, rgba(255,255,255,0.02) 10px, rgba(255,255,255,0.02) 11px);
      pointer-events: none;
    }
    .section-festival { background: var(--color-deep-black); position: relative; overflow: hidden; }
    .section-festival::after {
      content: ''; position: absolute; inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
      pointer-events: none; mix-blend-mode: overlay; z-index: 1;
    }
    .section-konser { background: var(--color-deep-black); position: relative; overflow: hidden; }
    .section-konser::after {
      content: ''; position: absolute; inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
      pointer-events: none; mix-blend-mode: overlay; z-index: 1;
    }
    .section-kepanitiaan { background: var(--color-ivory); }
    .section-rundown { background: var(--color-ivory-dark); }
    .section-registrasi { background: var(--color-crimson); position: relative; overflow: hidden; }
    .section-registrasi::before {
      content: ''; position: absolute; inset: 0;
      background-image:
        repeating-linear-gradient(45deg, transparent, transparent 10px, rgba(255,255,255,0.02) 10px, rgba(255,255,255,0.02) 11px),
        repeating-linear-gradient(-45deg, transparent, transparent 10px, rgba(255,255,255,0.02) 10px, rgba(255,255,255,0.02) 11px);
      pointer-events: none;
    }
    .section > .section-inner, .section > .section-inner--narrow { position: relative; z-index: 2; }

    /* ============================================================
       HERO
       ============================================================ */
    .hero {
      position: relative; min-height: 100vh; display: flex; align-items: center; justify-content: center;
      background: var(--color-deep-black); overflow: hidden;
      padding: var(--section-pad-x);
    }
    .hero-bg {
      position: absolute; inset: 0;
      background-image:
        repeating-linear-gradient(45deg, transparent, transparent 12px, rgba(201,168,76,0.04) 12px, rgba(201,168,76,0.04) 13px),
        repeating-linear-gradient(-45deg, transparent, transparent 12px, rgba(201,168,76,0.04) 12px, rgba(201,168,76,0.04) 13px);
    }
    .hero-noise {
      position: absolute; inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.06'/%3E%3C/svg%3E");
      pointer-events: none; mix-blend-mode: overlay; z-index: 1;
    }
    .hero-content {
      position: relative; z-index: 2; text-align: center; max-width: 900px; width: 100%;
    }
    .hero-label {
      font-family: var(--font-mono); font-size: 0.7rem; letter-spacing: 0.2em; text-transform: uppercase;
      color: var(--color-gold); margin-bottom: 2rem; opacity: 0.8;
      animation: fadeDown var(--dur-slow) var(--ease-power) 300ms both;
    }
    .hero-wordmark {
      font-family: var(--font-display); font-size: clamp(3.5rem, 10vw, 8rem); font-weight: 900;
      color: var(--color-text-on-dark); margin-bottom: 0.5rem; line-height: 1;
      animation: heroWordmark 1000ms var(--ease-power) 400ms both;
      background: linear-gradient(135deg, var(--color-gold-dark) 0%, var(--color-gold-light) 30%, var(--color-gold) 50%, var(--color-gold-light) 70%, var(--color-gold-dark) 100%);
      background-size: 200% auto;
      -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
      animation: heroWordmark 1000ms var(--ease-power) 400ms both, shimmer 4s linear 1500ms infinite;
    }
    .hero-subtitle {
      font-family: var(--font-mono); font-size: clamp(0.65rem, 1.2vw, 0.85rem);
      letter-spacing: 0.18em; text-transform: uppercase; color: var(--color-gold); margin-bottom: 1.5rem;
      animation: fadeUp var(--dur-slow) var(--ease-power) 900ms both;
    }
    .hero-divider {
      width: 80px; height: 1px; background: var(--color-gold); margin: 0 auto 1.5rem;
      animation: drawLine 600ms var(--ease-out) 1100ms both;
    }
    .hero-tagline-1, .hero-tagline-2 {
      font-family: var(--font-display); font-size: clamp(1.25rem, 3vw, 2.25rem);
      font-style: italic; color: var(--color-text-on-dark); line-height: 1.3; opacity: 0.85;
    }
    .hero-tagline-1 { animation: taglineReveal 700ms var(--ease-power) 1300ms both; }
    .hero-tagline-2 { animation: taglineReveal 700ms var(--ease-power) 1500ms both; margin-bottom: 2rem; }
    .hero-countdown {
      display: flex; align-items: center; justify-content: center; gap: 0.75rem; margin: 2.5rem 0;
      animation: scaleUp 500ms var(--ease-spring) 1800ms both;
    }
    .cd-block { display: flex; flex-direction: column; align-items: center; min-width: 70px; }
    .cd-number {
      font-family: var(--font-mono); font-size: clamp(2rem, 5vw, 4rem); font-weight: 700;
      color: var(--color-crimson); text-shadow: 0 0 40px rgba(139,26,26,0.3);
      line-height: 1;
    }
    .cd-label {
      font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.2em; text-transform: uppercase;
      color: rgba(201,168,76,0.6); margin-top: 0.25rem;
    }
    .cd-sep {
      font-family: var(--font-mono); font-size: clamp(1.5rem, 3vw, 2.5rem);
      color: var(--color-gold); margin-bottom: 1.5rem;
    }
    .hero-countdown .cd-sep:last-child { display: none; }
    .countdown-over { font-family: var(--font-mono); color: var(--color-gold); font-size: 1rem; letter-spacing: 0.15em; }
    .hero-buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
    .hero-btn-1 { animation: fadeUp var(--dur-slow) var(--ease-power) 2100ms both; }
    .hero-btn-2 { animation: fadeUp var(--dur-slow) var(--ease-power) 2250ms both; }
    .scroll-indicator {
      position: absolute; bottom: 2rem; left: 50%; transform: translateX(-50%); z-index: 2;
      color: var(--color-gold); font-size: 1.5rem; opacity: 0.6;
      animation: fadeIn 500ms ease 2400ms both, scrollBounce 2s ease-in-out 2900ms infinite;
    }

    @media (max-width: 600px) {
      .hero-countdown { gap: 0.25rem; }
      .cd-block { min-width: 55px; }
      .cd-number { font-size: clamp(1.75rem, 8vw, 2.5rem); }
      .hero-buttons { flex-direction: column; align-items: center; }
    }

    /* ============================================================
       BUTTONS
       ============================================================ */
    .btn-primary, .btn-secondary {
      display: inline-block; font-family: var(--font-mono); font-size: 0.75rem;
      letter-spacing: 0.1em; text-transform: uppercase; text-decoration: none;
      padding: 0.875rem 2rem; border: 2px solid; transition: all var(--dur-normal) var(--ease-out);
      cursor: pointer;
    }
    .btn-primary {
      background: var(--color-crimson); color: var(--color-ivory); border-color: var(--color-crimson);
    }
    .btn-primary:hover {
      background: var(--color-crimson-dark); border-color: var(--color-gold); color: var(--color-gold);
      transform: translateY(-2px); box-shadow: 0 8px 30px rgba(139,26,26,0.4);
    }
    .btn-secondary {
      background: transparent; color: var(--color-gold); border-color: var(--color-gold);
    }
    .btn-secondary:hover {
      background: var(--color-gold); color: var(--color-deep-black); transform: translateY(-2px);
    }

    /* ============================================================
       TENTANG
       ============================================================ */
    .tentang-divider { background: var(--color-crimson); }
    .tentang-lead strong { color: var(--color-crimson); }
    .tentang-quote {
      text-align: center; margin: 3rem auto; max-width: 700px; position: relative;
      padding: 2.5rem 2rem;
      border-top: 2px solid var(--color-border); border-bottom: 2px solid var(--color-border);
    }
    .tentang-quote-mark {
      font-family: var(--font-display); font-size: 5rem; color: var(--color-gold);
      line-height: 0; position: absolute; top: 1.5rem; left: 50%; transform: translateX(-50%);
      opacity: 0.2;
    }
    .tentang-quote p {
      font-family: var(--font-display); font-size: clamp(1.25rem, 2.5vw, 1.75rem);
      font-style: italic; color: var(--color-crimson); position: relative; z-index: 1;
    }
    .tentang-values { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; margin-top: 3rem; }
    .value-card {
      background: var(--color-paper); padding: 2rem 1.5rem; text-align: center;
      border: 1px solid var(--color-border); position: relative; overflow: hidden;
      transition: transform var(--dur-normal) var(--ease-spring), box-shadow var(--dur-normal) var(--ease-out);
    }
    .value-card:hover {
      transform: translateY(-4px); box-shadow: 0 12px 40px rgba(139,26,26,0.1);
    }
    .value-number {
      position: absolute; top: 0.5rem; right: 0.75rem;
      font-family: var(--font-mono); font-size: 3rem; font-weight: 700;
      color: rgba(139,26,26,0.06); line-height: 1;
    }
    .value-title { font-family: var(--font-display); font-size: 1.25rem; font-weight: 700; color: var(--color-crimson); margin-bottom: 0.75rem; }
    .value-divider { width: 40px; height: 2px; background: var(--color-gold); margin: 0 auto 1rem; }
    .value-desc { font-size: 0.9375rem; color: var(--color-text-secondary); line-height: 1.6; }

    @media (max-width: 767px) {
      .tentang-values { grid-template-columns: 1fr; }
    }
    @media (min-width: 768px) and (max-width: 1023px) {
      .tentang-values { grid-template-columns: 1fr 1fr; }
    }

    /* ============================================================
       NILAI DASAR
       ============================================================ */
    .nilai-label, .nilai-title, .nilai-lead { color: var(--color-text-on-dark) !important; }
    .nilai-divider { background: var(--color-gold); }
    .nilai-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.25rem; }
    .nilai-card {
      background: var(--color-paper); padding: 2rem 1.5rem; position: relative; overflow: hidden;
      border: 1px solid var(--color-border); cursor: default;
      transition: transform var(--dur-normal) var(--ease-spring), box-shadow var(--dur-normal) var(--ease-out), border-color var(--dur-normal) var(--ease-out);
    }
    .nilai-card:hover {
      transform: translateY(-6px); box-shadow: 0 16px 48px rgba(0,0,0,0.25);
      border-color: var(--color-gold);
    }
    .nilai-card-number {
      font-family: var(--font-mono); font-size: 3rem; font-weight: 700;
      color: rgba(139,26,26,0.06); position: absolute; top: 0.5rem; right: 0.75rem; line-height: 1;
      transition: color var(--dur-normal) ease, transform var(--dur-normal) var(--ease-spring);
    }
    .nilai-card:hover .nilai-card-number { color: rgba(201,168,76,0.15); }
    .nilai-card-title {
      font-family: var(--font-display); font-size: 1.125rem; font-weight: 700;
      color: var(--color-crimson); margin-bottom: 0.75rem; position: relative; z-index: 1;
    }
    .nilai-card-divider { width: 40px; height: 2px; background: var(--color-gold); margin-bottom: 0.75rem; }
    .nilai-card-desc { font-size: 0.875rem; color: var(--color-text-secondary); line-height: 1.6; position: relative; z-index: 1; }

    @media (max-width: 767px) {
      .nilai-grid { grid-template-columns: 1fr; }
    }
    @media (min-width: 768px) and (max-width: 1023px) {
      .nilai-grid { grid-template-columns: repeat(2, 1fr); }
    }

    /* ============================================================
       KEGIATAN (TIMELINE)
       ============================================================ */
    .kegiatan-divider { background: var(--color-crimson); }
    .timeline { position: relative; padding-left: 2rem; }
    .timeline::before {
      content: ''; position: absolute; left: 11px; top: 0; bottom: 0;
      width: 2px; background: var(--color-crimson);
    }
    .timeline-item { position: relative; margin-bottom: 2.5rem; }
    .timeline-marker { position: absolute; left: -2rem; top: 0.5rem; }
    .timeline-dot {
      width: 24px; height: 24px; border-radius: 50%;
      background: var(--color-crimson); border: 3px solid var(--color-gold);
    }
    .timeline-card {
      background: var(--color-paper); padding: 1.5rem; border-left: 3px solid var(--color-crimson);
      box-shadow: 0 2px 12px rgba(0,0,0,0.06);
      transition: transform var(--dur-normal) var(--ease-spring), box-shadow var(--dur-normal) var(--ease-out);
    }
    .timeline-card:hover {
      transform: translateY(-3px); box-shadow: 0 8px 24px rgba(0,0,0,0.1);
    }
    .timeline-day { font-family: var(--font-display); font-size: 1.25rem; font-weight: 700; color: var(--color-crimson); margin-bottom: 0.25rem; }
    .timeline-date { font-family: var(--font-mono); font-size: 0.75rem; color: var(--color-gold-dark); letter-spacing: 0.1em; text-transform: uppercase; margin-bottom: 1rem; }
    .timeline-events { list-style: none; }
    .timeline-events li {
      padding: 0.35rem 0; font-size: 0.9375rem; color: var(--color-text-secondary);
      border-bottom: 1px solid var(--color-border); display: flex; gap: 1rem;
    }
    .timeline-events li:last-child { border-bottom: none; }
    .time {
      font-family: var(--font-mono); font-size: 0.75rem; color: var(--color-gold-dark);
      white-space: nowrap; min-width: 85px; font-weight: 700;
    }

    @media (min-width: 768px) {
      .timeline { padding-left: 0; }
      .timeline::before { left: 50%; margin-left: -1px; }
      .timeline-item { width: 50%; }
      .timeline-item:nth-child(odd) { margin-left: 0; padding-right: 2.5rem; }
      .timeline-item:nth-child(odd) .timeline-marker { left: auto; right: -13px; }
      .timeline-item:nth-child(even) { margin-left: 50%; padding-left: 2.5rem; }
      .timeline-item:nth-child(even) .timeline-marker { left: -13px; }
    }

    /* ============================================================
       KOMISI
       ============================================================ */
    .komisi-label, .komisi-title, .komisi-lead { color: var(--color-text-on-dark) !important; }
    .komisi-divider { background: var(--color-gold); }
    .komisi-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.25rem; position: relative; z-index: 2; }
    .komisi-card {
      background: var(--color-charcoal); padding: 1.75rem; border-top: 3px solid var(--color-crimson);
      display: flex; flex-direction: column;
      transition: transform var(--dur-normal) var(--ease-spring), box-shadow var(--dur-normal) var(--ease-out), border-color var(--dur-normal) var(--ease-out);
    }
    .komisi-card:hover {
      transform: translateY(-4px); box-shadow: 0 12px 40px rgba(139,26,26,0.25);
      border-color: var(--color-gold);
    }
    .komisi-card-top { margin-bottom: 1rem; }
    .komisi-number {
      font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.2em;
      color: var(--color-gold); text-transform: uppercase; margin-bottom: 0.5rem;
      transition: color var(--dur-normal) ease, transform var(--dur-normal) var(--ease-spring);
    }
    .komisi-card:hover .komisi-number { color: var(--color-gold-light); }
    .komisi-title-card { font-family: var(--font-display); font-size: 1.125rem; font-weight: 700; color: var(--color-text-on-dark); }
    .komisi-desc { font-size: 0.8125rem; color: rgba(245,240,232,0.6); line-height: 1.7; margin-bottom: 1rem; flex-grow: 1; }
    .komisi-tags { display: flex; flex-wrap: wrap; gap: 0.5rem; }
    .tag {
      font-family: var(--font-mono); font-size: 0.6rem; letter-spacing: 0.05em;
      color: rgba(201,168,76,0.5); background: rgba(201,168,76,0.08);
      padding: 0.3rem 0.65rem; border: 1px solid rgba(201,168,76,0.1);
    }

    @media (max-width: 767px) {
      .komisi-grid { grid-template-columns: 1fr; }
    }
    @media (min-width: 768px) and (max-width: 1023px) {
      .komisi-grid { grid-template-columns: repeat(2, 1fr); }
    }

    /* ============================================================
       OUTPUT
       ============================================================ */
    .output-divider { background: var(--color-crimson); }
    .output-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.25rem; }
    .output-card {
      background: var(--color-paper); padding: 1.75rem; border: 1px solid var(--color-border);
      border-left: 4px solid var(--color-crimson); position: relative;
      transition: transform var(--dur-normal) var(--ease-spring), box-shadow var(--dur-normal) var(--ease-out);
    }
    .output-card:hover {
      transform: translateY(-4px); box-shadow: 0 8px 24px rgba(139,26,26,0.1);
    }
    .output-number {
      font-family: var(--font-mono); font-size: 0.7rem; letter-spacing: 0.15em;
      color: var(--color-gold-dark); text-transform: uppercase; margin-bottom: 0.5rem;
    }
    .output-title {
      font-family: var(--font-display); font-size: 1.125rem; font-weight: 700;
      color: var(--color-text-primary); margin-bottom: 0.5rem;
    }
    .output-desc { font-size: 0.875rem; color: var(--color-text-secondary); line-height: 1.6; }

    @media (max-width: 767px) {
      .output-grid { grid-template-columns: 1fr; }
    }

    /* ============================================================
       REPRESENTASI
       ============================================================ */
    .rep-label, .rep-title, .rep-lead { color: var(--color-text-on-dark) !important; }
    .rep-divider { background: var(--color-gold); }
    .rep-grid {
      display: flex; flex-wrap: wrap; justify-content: center; gap: 1rem; margin-top: 2rem;
      position: relative; z-index: 2;
    }
    .rep-item {
      display: flex; flex-direction: column; align-items: center; gap: 0.5rem;
      background: rgba(250,247,242,0.1); border: 1px solid rgba(201,168,76,0.15);
      padding: 1.25rem 1.5rem; min-width: 120px; text-align: center;
      color: var(--color-text-on-dark); font-size: 0.85rem; font-weight: 600;
      transition: transform var(--dur-normal) var(--ease-spring), background var(--dur-normal) var(--ease-out);
    }
    .rep-item:hover {
      transform: translateY(-4px); background: rgba(250,247,242,0.18);
    }
    .rep-icon { font-size: 1.5rem; }

    /* ============================================================
       FESTIVAL
       ============================================================ */
    .festival-label, .festival-title, .festival-lead { color: var(--color-text-on-dark) !important; }
    .festival-divider { background: var(--color-gold); }
    .festival-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.25rem; position: relative; z-index: 2; }
    .festival-card {
      background: var(--color-charcoal); padding: 2rem 1.5rem; text-align: center;
      border: 1px solid rgba(201,168,76,0.08);
      transition: transform var(--dur-normal) var(--ease-spring), box-shadow var(--dur-normal) var(--ease-out), border-color var(--dur-normal) var(--ease-out);
    }
    .festival-card:hover {
      transform: translateY(-6px); box-shadow: 0 12px 40px rgba(139,26,26,0.2);
      border-color: rgba(201,168,76,0.2);
    }
    .festival-icon { font-size: 2rem; margin-bottom: 1rem; }
    .festival-card-title {
      font-family: var(--font-display); font-size: 1.125rem; font-weight: 700;
      color: var(--color-text-on-dark); margin-bottom: 0.75rem;
    }
    .festival-card-desc { font-size: 0.8125rem; color: rgba(245,240,232,0.6); line-height: 1.7; }

    @media (max-width: 767px) {
      .festival-grid { grid-template-columns: 1fr; }
    }
    @media (min-width: 768px) and (max-width: 1023px) {
      .festival-grid { grid-template-columns: repeat(2, 1fr); }
    }

    /* ============================================================
       KONSER
       ============================================================ */
    .konser-label, .konser-title, .konser-lead { color: var(--color-text-on-dark) !important; }
    .konser-divider { background: var(--color-gold); }
    .konser-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.25rem; position: relative; z-index: 2; }
    .konser-card {
      background: var(--color-charcoal); padding: 2rem 1.5rem; text-align: center;
      border: 1px solid rgba(201,168,76,0.08);
      transition: transform var(--dur-normal) var(--ease-spring), box-shadow var(--dur-normal) var(--ease-out);
    }
    .konser-card:hover {
      transform: translateY(-4px); box-shadow: 0 12px 36px rgba(139,26,26,0.2);
    }
    .konser-card-featured {
      border: 1px solid var(--color-gold);
      background: linear-gradient(180deg, rgba(201,168,76,0.08) 0%, var(--color-charcoal) 100%);
    }
    .konser-card-order {
      font-family: var(--font-mono); font-size: 0.6rem; letter-spacing: 0.2em; text-transform: uppercase;
      color: var(--color-gold); margin-bottom: 0.75rem;
    }
    .konser-card-name {
      font-family: var(--font-display); font-size: 1.125rem; font-weight: 700;
      color: var(--color-text-on-dark); margin-bottom: 0.5rem;
    }
    .konser-card-genre { font-size: 0.75rem; color: rgba(245,240,232,0.5); }
    .konser-note {
      text-align: center; font-size: 0.75rem; color: rgba(245,240,232,0.3);
      margin-top: 2rem; font-style: italic; position: relative; z-index: 2;
    }

    @media (max-width: 767px) {
      .konser-grid { grid-template-columns: 1fr; }
    }
    @media (min-width: 768px) and (max-width: 1023px) {
      .konser-grid { grid-template-columns: repeat(2, 1fr); }
    }

    /* ============================================================
       KEPANITIAAN
       ============================================================ */
    .panitia-divider { background: var(--color-crimson); }
    .panitia-stack { display: flex; flex-direction: column; gap: 2rem; }
    .panitia-level {
      background: var(--color-paper); padding: 2rem; border: 1px solid var(--color-border);
      border-left: 4px solid var(--color-crimson);
      transition: transform var(--dur-normal) var(--ease-spring), box-shadow var(--dur-normal) var(--ease-out);
    }
    .panitia-level:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,0,0,0.06); }
    .panitia-level-1 { border-left-color: var(--color-gold); }
    .panitia-level-label {
      font-family: var(--font-display); font-size: 1rem; font-weight: 700;
      color: var(--color-crimson); text-transform: uppercase; letter-spacing: 0.1em;
      margin-bottom: 1rem; padding-bottom: 0.75rem;
      border-bottom: 2px solid var(--color-border-gold);
    }
    .panitia-level-1 .panitia-level-label { color: var(--color-gold-dark); }
    .panitia-names { display: flex; flex-wrap: wrap; gap: 0.75rem 1.5rem; }
    .panitia-name {
      font-size: 0.9375rem; color: var(--color-text-primary); font-weight: 600;
    }
    .panitia-name.big { font-size: 1.125rem; font-weight: 700; }
    .panitia-name.dim { color: var(--color-text-muted); font-weight: 400; font-style: italic; font-size: 0.875rem; }
    .panitia-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1rem; }
    .panitia-role { display: flex; flex-direction: column; }
    .role-title { font-size: 0.75rem; color: var(--color-text-muted); text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 0.25rem; }
    .role-name { font-size: 1rem; font-weight: 700; color: var(--color-text-primary); }

    @media (max-width: 767px) {
      .panitia-grid { grid-template-columns: 1fr; }
      .panitia-names { flex-direction: column; gap: 0.5rem; }
    }

    /* ============================================================
       RUNDOWN
       ============================================================ */
    .rundown-divider { background: var(--color-gold-dark); }
    .rundown-timeline { display: flex; flex-direction: column; gap: 1px; }
    .rundown-item {
      display: flex; align-items: baseline; gap: 1.5rem; padding: 1rem 1.25rem;
      background: var(--color-paper); transition: background var(--dur-fast);
    }
    .rundown-item:nth-child(even) { background: rgba(139,26,26,0.02); }
    .rundown-item:hover { background: rgba(201,168,76,0.05); }
    .rundown-item-highlight {
      background: rgba(139,26,26,0.06) !important;
      border-left: 4px solid var(--color-gold);
    }
    .rundown-time {
      font-family: var(--font-mono); font-size: 0.85rem; font-weight: 700;
      color: var(--color-crimson); white-space: nowrap; min-width: 130px;
    }
    .rundown-event { font-size: 0.9375rem; color: var(--color-text-primary); }
    @media (max-width: 600px) {
      .rundown-item { flex-direction: column; gap: 0.25rem; }
      .rundown-time { min-width: auto; }
    }

    /* ============================================================
       REGISTRASI
       ============================================================ */
    .registrasi-label, .registrasi-title, .registrasi-lead { color: var(--color-text-on-dark) !important; }
    .registrasi-divider { background: var(--color-gold); }
    .registrasi-quote {
      text-align: center; margin: 0 auto 2rem; position: relative; z-index: 2;
    }
    .registrasi-quote p {
      font-family: var(--font-display); font-size: clamp(1rem, 2vw, 1.375rem);
      font-style: italic; color: var(--color-gold-light); opacity: 0.8;
    }
    .registrasi-form {
      background: var(--color-paper); padding: 2.5rem; max-width: 560px;
      margin: 0 auto; position: relative; z-index: 2;
    }
    .form-group { margin-bottom: 1.25rem; }
    .form-group label {
      display: block; font-family: var(--font-mono); font-size: 0.7rem;
      letter-spacing: 0.1em; text-transform: uppercase; color: var(--color-text-secondary);
      margin-bottom: 0.4rem;
    }
    .required { color: var(--color-crimson); }
    .form-group input, .form-group select, .form-group textarea {
      width: 100%; padding: 0.75rem 1rem; font-family: var(--font-body); font-size: 1rem;
      color: var(--color-text-primary); background: var(--color-ivory);
      border: 1px solid var(--color-border); transition: border-color var(--dur-fast), box-shadow var(--dur-fast);
    }
    .form-group input:focus, .form-group select:focus, .form-group textarea:focus {
      outline: none; border-color: var(--color-crimson); box-shadow: 0 0 0 2px rgba(139,26,26,0.1);
    }
    .form-group textarea { resize: vertical; }
    .btn-submit {
      width: 100%; padding: 1rem; font-family: var(--font-mono); font-size: 0.8rem;
      letter-spacing: 0.15em; text-transform: uppercase; border: 2px solid var(--color-gold);
      background: var(--color-gold); color: var(--color-deep-black); cursor: pointer;
      transition: all var(--dur-normal) var(--ease-out); font-weight: 700;
    }
    .btn-submit:hover {
      background: var(--color-gold-light); border-color: var(--color-gold-light);
      transform: translateY(-2px); box-shadow: 0 8px 24px rgba(201,168,76,0.3);
    }
    .form-thanks {
      text-align: center; padding: 3rem 2rem; max-width: 560px; margin: 0 auto;
      background: var(--color-paper); position: relative; z-index: 2;
    }
    .thanks-icon {
      width: 60px; height: 60px; border-radius: 50%; background: var(--color-gold);
      color: var(--color-deep-black); font-size: 1.5rem; display: flex;
      align-items: center; justify-content: center; margin: 0 auto 1rem;
    }
    .thanks-title { font-family: var(--font-display); font-size: 1.5rem; font-weight: 700; color: var(--color-crimson); margin-bottom: 0.5rem; }
    .thanks-desc { font-size: 0.9375rem; color: var(--color-text-secondary); }
    .registrasi-alt {
      text-align: center; font-size: 0.875rem; color: rgba(245,240,232,0.6);
      margin-top: 1.5rem; position: relative; z-index: 2;
    }
    .registrasi-alt strong { color: var(--color-gold-light); }

    /* ============================================================
       FOOTER
       ============================================================ */
    .footer {
      background: var(--color-deep-black); padding: var(--section-pad-y) var(--section-pad-x);
      color: var(--color-text-on-dark); position: relative; overflow: hidden;
    }
    .footer::after {
      content: ''; position: absolute; inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.03'/%3E%3C/svg%3E");
      pointer-events: none; mix-blend-mode: overlay; z-index: 1;
    }
    .footer > .section-inner { position: relative; z-index: 2; }
    .footer-grid {
      display: grid; grid-template-columns: 2fr 1fr 1fr; gap: 3rem; margin-bottom: 3rem;
    }
    .footer-logo {
      font-family: var(--font-mono); font-size: 1.25rem; font-weight: 700;
      color: var(--color-gold); margin-bottom: 1rem; letter-spacing: 0.1em;
    }
    .footer-desc { font-size: 0.9375rem; color: rgba(245,240,232,0.7); margin-bottom: 0.5rem; }
    .footer-info { font-size: 0.8125rem; color: rgba(245,240,232,0.4); font-family: var(--font-mono); }
    .footer-heading {
      font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.15em;
      text-transform: uppercase; color: var(--color-gold); margin-bottom: 1rem; opacity: 0.7;
    }
    .footer-social-heading { margin-top: 1.5rem; }
    .footer-nav ul, .footer-contact ul { list-style: none; }
    .footer-nav li, .footer-contact li { margin-bottom: 0.5rem; }
    .footer-nav a, .footer-contact a {
      color: rgba(245,240,232,0.6); text-decoration: none; font-size: 0.875rem;
      transition: color var(--dur-fast);
    }
    .footer-nav a:hover, .footer-contact a:hover { color: var(--color-gold); }
    .footer-contact li { font-size: 0.875rem; color: rgba(245,240,232,0.5); }
    .footer-divider {
      height: 1px; background: linear-gradient(90deg, transparent, var(--color-gold), transparent);
      margin: 2rem 0;
    }
    .footer-quote {
      text-align: center; font-family: var(--font-display); font-size: 1.125rem;
      font-style: italic; color: rgba(201,168,76,0.5); margin: 2rem 0;
    }
    .footer-copy {
      text-align: center; font-size: 0.8125rem; color: rgba(245,240,232,0.4);
    }
    .footer-copy em { color: var(--color-gold-dark); font-style: normal; }
    .footer-note {
      text-align: center; font-size: 0.6875rem; color: rgba(245,240,232,0.25);
      margin-top: 1rem;
    }

    @media (max-width: 767px) {
      .footer-grid { grid-template-columns: 1fr; gap: 2rem; }
    }

    /* ============================================================
       MOBILE ANIMATION ADJUSTMENTS
       ============================================================ */
    @media (max-width: 767px) {
      [data-animate="fade-up"] { transform: translateY(25px); }
      [data-animate="slide-left"] { transform: translateX(-30px); }
      [data-animate="slide-right"] { transform: translateX(30px); }
      [data-animate] { transition-duration: var(--dur-normal); }
    }

    /* ============================================================
       HERO SECTION
       ============================================================ */
    .hero {
      position: relative; min-height: 100vh; display: flex;
      align-items: center; justify-content: center; overflow: hidden;
      background: var(--color-deep-black);
    }
    .hero-bg {
      position: absolute; inset: 0; z-index: 0;
      background:
        repeating-linear-gradient(45deg, transparent, transparent 18px, rgba(201,168,76,0.03) 18px, rgba(201,168,76,0.03) 19px),
        repeating-linear-gradient(-45deg, transparent, transparent 18px, rgba(201,168,76,0.03) 18px, rgba(201,168,76,0.03) 19px);
    }
    .hero-noise {
      position: absolute; inset: 0; z-index: 1; pointer-events: none;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.035'/%3E%3C/svg%3E");
      mix-blend-mode: overlay;
    }
    .hero-content {
      position: relative; z-index: 2; text-align: center;
      padding: 6rem var(--section-pad-x) 4rem; max-width: 900px;
    }
    .hero-label {
      font-family: var(--font-mono); font-size: 0.75rem; color: var(--color-gold);
      letter-spacing: 0.2em; text-transform: uppercase; margin-bottom: 1.5rem;
      animation: fadeDown var(--dur-slow) var(--ease-power) 300ms both;
    }
    .hero-wordmark {
      font-family: var(--font-display);
      font-size: clamp(3.5rem, 10vw, 8rem);
      font-weight: 900; color: var(--color-ivory); line-height: 1;
      margin-bottom: 1rem;
      animation: heroWordmark 1000ms var(--ease-power) 400ms both;
      background: linear-gradient(90deg, var(--color-gold-dark) 0%, var(--color-gold-light) 40%, var(--color-gold) 50%, var(--color-gold-light) 60%, var(--color-gold-dark) 100%);
      background-size: 200% auto;
      -webkit-background-clip: text; -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: heroWordmark 1000ms var(--ease-power) 400ms both, shimmer 4s linear 2s infinite;
    }
    .hero-subtitle {
      font-family: var(--font-mono); font-size: clamp(0.65rem, 1.2vw, 0.85rem);
      color: var(--color-gold); letter-spacing: 0.15em; text-transform: uppercase;
      margin-bottom: 2rem; opacity: 0.85;
      animation: fadeUp var(--dur-slow) var(--ease-power) 900ms both;
    }
    .hero-divider {
      width: 80px; height: 2px; background: var(--color-gold);
      margin: 0 auto 2rem;
      animation: drawLine 600ms var(--ease-out) 1100ms both;
    }
    .hero-tagline-1, .hero-tagline-2 {
      font-family: var(--font-display); font-style: italic;
      font-size: clamp(1.25rem, 3vw, 2rem);
      color: var(--color-ivory); line-height: 1.3;
    }
    .hero-tagline-1 { animation: taglineReveal 700ms var(--ease-power) 1300ms both; }
    .hero-tagline-2 { animation: taglineReveal 700ms var(--ease-power) 1500ms both; }

    /* Countdown */
    .hero-countdown {
      display: flex; align-items: center; justify-content: center;
      gap: 0.75rem; margin: 2.5rem 0;
      animation: scaleUp 500ms var(--ease-spring) 1800ms both;
    }
    .cd-block {
      background: rgba(13,13,13,0.6); border: 1px solid rgba(201,168,76,0.2);
      backdrop-filter: blur(8px); padding: 1rem 1.25rem; min-width: 70px;
      text-align: center;
    }
    .cd-number {
      font-family: var(--font-mono); font-size: clamp(1.5rem, 3vw, 2.5rem);
      font-weight: 700; color: var(--color-gold); display: block; line-height: 1;
    }
    .cd-label {
      font-family: var(--font-mono); font-size: 0.6rem; color: rgba(245,240,232,0.5);
      text-transform: uppercase; letter-spacing: 0.15em; margin-top: 0.5rem; display: block;
    }
    .cd-sep {
      font-family: var(--font-mono); font-size: 1.5rem; color: var(--color-gold);
      animation: colonPulse 1s ease-in-out infinite; align-self: flex-start; margin-top: 0.5rem;
    }
    @keyframes colonPulse {
      0%, 100% { opacity: 1; } 50% { opacity: 0.3; }
    }

    /* Hero Buttons */
    .hero-buttons {
      display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;
    }
    .hero-btn-1 { animation: fadeUp var(--dur-slow) var(--ease-power) 2100ms both; }
    .hero-btn-2 { animation: fadeUp var(--dur-slow) var(--ease-power) 2250ms both; }

    .scroll-indicator {
      position: absolute; bottom: 2rem; left: 50%; transform: translateX(-50%);
      color: var(--color-gold); font-size: 1.5rem; z-index: 2;
      animation: fadeIn 500ms ease 2400ms both, scrollBounce 2s ease-in-out 2900ms infinite;
    }

    /* ============================================================
       BUTTONS
       ============================================================ */
    .btn-primary {
      display: inline-block; background: var(--color-crimson);
      color: var(--color-ivory); font-family: var(--font-mono);
      font-size: 0.75rem; letter-spacing: 0.1em; text-transform: uppercase;
      padding: 0.875rem 2rem; border: 2px solid var(--color-crimson);
      text-decoration: none; cursor: pointer; transition: all var(--dur-normal) var(--ease-out);
    }
    .btn-primary:hover {
      background: var(--color-crimson-dark); border-color: var(--color-gold);
      color: var(--color-gold); transform: translateY(-2px);
      box-shadow: 0 8px 30px rgba(139,26,26,0.4);
    }
    .btn-secondary {
      display: inline-block; background: transparent;
      color: var(--color-gold); font-family: var(--font-mono);
      font-size: 0.75rem; letter-spacing: 0.1em; text-transform: uppercase;
      padding: 0.875rem 2rem; border: 2px solid var(--color-gold);
      text-decoration: none; cursor: pointer; transition: all var(--dur-normal) var(--ease-out);
    }
    .btn-secondary:hover {
      background: var(--color-gold); color: var(--color-deep-black);
      transform: translateY(-2px);
    }

    /* ============================================================
       SECTION BACKGROUNDS
       ============================================================ */
    .section-tentang { background: var(--color-ivory); }
    .section-nilai { background: var(--color-crimson); color: var(--color-text-on-dark); }
    .section-nilai .section-label { color: var(--color-gold); }
    .section-nilai .section-title { color: var(--color-text-on-dark); }
    .section-nilai .section-lead { color: rgba(245,240,232,0.8); }
    .section-nilai .section-divider { background: var(--color-gold); }
    .section-kegiatan { background: var(--color-ivory-dark); }
    .section-komisi { background: var(--color-deep-black); color: var(--color-text-on-dark); }
    .section-komisi .section-label { color: var(--color-gold); }
    .section-komisi .section-title { color: var(--color-text-on-dark); }
    .section-komisi .section-lead { color: rgba(245,240,232,0.7); }
    .section-komisi .section-divider { background: var(--color-gold); }
    .section-output { background: var(--color-ivory); }
    .section-representasi { background: var(--color-crimson); color: var(--color-text-on-dark); }
    .section-representasi .section-label { color: var(--color-gold); }
    .section-representasi .section-title { color: var(--color-text-on-dark); }
    .section-representasi .section-lead { color: rgba(245,240,232,0.8); }
    .section-representasi .section-divider { background: var(--color-gold); }
    .section-festival { background: var(--color-deep-black); color: var(--color-text-on-dark); }
    .section-festival .section-label { color: var(--color-gold); }
    .section-festival .section-title { color: var(--color-text-on-dark); }
    .section-festival .section-lead { color: rgba(245,240,232,0.7); }
    .section-festival .section-divider { background: var(--color-gold); }
    .section-konser { background: var(--color-deep-black); color: var(--color-text-on-dark); }
    .section-konser .section-label { color: var(--color-gold); }
    .section-konser .section-title { color: var(--color-text-on-dark); }
    .section-konser .section-lead { color: rgba(245,240,232,0.7); }
    .section-konser .section-divider { background: var(--color-gold); }
    .section-kepanitiaan { background: var(--color-ivory); }
    .section-rundown { background: var(--color-ivory-dark); }
    .section-registrasi { background: var(--color-crimson); color: var(--color-text-on-dark); }
    .section-registrasi .section-label { color: var(--color-gold); }
    .section-registrasi .section-title { color: var(--color-text-on-dark); }
    .section-registrasi .section-lead { color: rgba(245,240,232,0.8); }
    .section-registrasi .section-divider { background: var(--color-gold); }

    /* Dark section noise overlay */
    .section-komisi, .section-festival, .section-konser { position: relative; }
    .section-komisi::before, .section-festival::before, .section-konser::before {
      content: ''; position: absolute; inset: 0; pointer-events: none;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.025'/%3E%3C/svg%3E");
      mix-blend-mode: overlay; z-index: 0;
    }
    .section-komisi > *, .section-festival > *, .section-konser > * { position: relative; z-index: 1; }

    /* ============================================================
       SECTION LABEL / TITLE / DIVIDER COLOR OVERRIDES
       ============================================================ */
    .section-tentang .section-label { color: var(--color-crimson); }
    .section-tentang .section-divider { background: var(--color-crimson); }
    .section-output .section-label { color: var(--color-crimson); }
    .section-output .section-divider { background: var(--color-crimson); }
    .section-kepanitiaan .section-label { color: var(--color-crimson); }
    .section-kepanitiaan .section-divider { background: var(--color-crimson); }
    .section-kegiatan .section-label { color: var(--color-crimson); }
    .section-kegiatan .section-divider { background: var(--color-crimson); }
    .section-rundown .section-label { color: var(--color-crimson); }
    .section-rundown .section-divider { background: var(--color-crimson); }

    /* ============================================================
       TENTANG SECTION
       ============================================================ */
    .tentang-divider { background: var(--color-crimson) !important; }
    .tentang-quote {
      text-align: center; margin: 2.5rem 0; position: relative;
    }
    .tentang-quote-mark {
      font-family: var(--font-display); font-size: 5rem; color: var(--color-gold);
      line-height: 0.8; display: block; margin-bottom: -1rem;
    }
    .tentang-quote p {
      font-family: var(--font-display); font-style: italic;
      font-size: clamp(1.25rem, 3vw, 2rem); color: var(--color-crimson);
      line-height: 1.4;
    }
    .tentang-values {
      display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; margin-top: 3rem;
    }
    .value-card {
      text-align: center; padding: 2rem 1.5rem;
      background: var(--color-paper); border: 1px solid var(--color-border);
      transition: all var(--dur-normal) var(--ease-spring);
    }
    .value-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 16px 48px rgba(139,26,26,0.15);
      border-color: var(--color-gold);
    }
    .value-number {
      font-family: var(--font-mono); font-size: 2.5rem; font-weight: 700;
      color: var(--color-crimson); opacity: 0.15; line-height: 1; margin-bottom: 0.5rem;
    }
    .value-title {
      font-family: var(--font-display); font-size: 1.25rem; font-weight: 700;
      color: var(--color-crimson); margin-bottom: 0.5rem;
    }
    .value-divider {
      width: 40px; height: 2px; background: var(--color-gold); margin: 0.75rem auto;
    }
    .value-desc { font-size: 0.95rem; color: var(--color-text-secondary); }

    @media (max-width: 767px) {
      .tentang-values { grid-template-columns: 1fr; }
    }

    /* ============================================================
       NILAI DASAR CARDS
       ============================================================ */
    .nilai-grid {
      display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.5rem;
    }
    .nilai-card {
      background: var(--color-paper); padding: 2rem 1.5rem; position: relative;
      border: 1px solid rgba(245,240,232,0.1);
      transition: all var(--dur-normal) var(--ease-spring);
    }
    .nilai-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 16px 48px rgba(0,0,0,0.3);
      border-color: var(--color-gold);
    }
    .nilai-card-number {
      font-family: var(--font-mono); font-size: 3rem; font-weight: 700;
      color: var(--color-crimson); opacity: 0.08; position: absolute;
      top: 0.75rem; right: 1rem; line-height: 1;
    }
    .nilai-card-title {
      font-family: var(--font-display); font-size: 1.15rem; font-weight: 700;
      color: var(--color-crimson); margin-bottom: 0.5rem;
    }
    .nilai-card-divider {
      width: 30px; height: 2px; background: var(--color-gold); margin: 0.75rem 0;
    }
    .nilai-card-desc { font-size: 0.9rem; color: var(--color-text-secondary); line-height: 1.6; }
    .nilai-divider { background: var(--color-gold) !important; }
    .nilai-label { color: var(--color-gold) !important; }

    @media (max-width: 1023px) { .nilai-grid { grid-template-columns: repeat(2, 1fr); } }
    @media (max-width: 767px) { .nilai-grid { grid-template-columns: 1fr; } }

    /* ============================================================
       TIMELINE (KEGIATAN)
       ============================================================ */
    .kegiatan-divider { background: var(--color-crimson) !important; }
    .timeline { position: relative; padding: 1rem 0; }
    .timeline::before {
      content: ''; position: absolute; left: 50%; top: 0; bottom: 0;
      width: 2px; background: var(--color-crimson); transform: translateX(-50%);
    }
    .timeline-item {
      display: flex; align-items: flex-start; margin-bottom: 2rem; position: relative;
    }
    .timeline-item:nth-child(odd) { flex-direction: row-reverse; }
    .timeline-marker {
      flex-shrink: 0; width: 100px; display: flex; justify-content: center; position: relative; z-index: 1;
    }
    .timeline-dot {
      width: 14px; height: 14px; border-radius: 50%; background: var(--color-crimson);
      border: 3px solid var(--color-gold); margin-top: 0.5rem;
    }
    .timeline-card {
      flex: 1; max-width: 460px; background: var(--color-paper);
      border-left: 3px solid var(--color-crimson); padding: 1.5rem;
      box-shadow: 0 2px 12px rgba(0,0,0,0.06);
    }
    .timeline-item:nth-child(odd) .timeline-card { margin-right: calc(50% + 50px); }
    .timeline-item:nth-child(even) .timeline-card { margin-left: calc(50% + 50px); }
    .timeline-day {
      font-family: var(--font-mono); font-size: 0.7rem; color: var(--color-crimson);
      text-transform: uppercase; letter-spacing: 0.15em; font-weight: 700;
    }
    .timeline-date {
      font-family: var(--font-display); font-size: 1.25rem; font-weight: 700;
      color: var(--color-text-primary); margin: 0.25rem 0 1rem;
    }
    .timeline-events { list-style: none; padding: 0; }
    .timeline-events li {
      padding: 0.35rem 0; font-size: 0.95rem; color: var(--color-text-secondary);
      border-bottom: 1px solid var(--color-border);
    }
    .timeline-events li:last-child { border-bottom: none; }
    .timeline-events .time {
      font-family: var(--font-mono); font-size: 0.75rem; color: var(--color-gold);
      margin-right: 0.5rem; font-weight: 700;
    }

    @media (max-width: 767px) {
      .timeline::before { left: 20px; }
      .timeline-item, .timeline-item:nth-child(odd) { flex-direction: row; }
      .timeline-marker { width: 40px; }
      .timeline-card, .timeline-item:nth-child(odd) .timeline-card, .timeline-item:nth-child(even) .timeline-card {
        margin-left: 50px; margin-right: 0; max-width: none;
      }
    }

    /* ============================================================
       KOMISI CARDS
       ============================================================ */
    .komisi-grid {
      display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem;
    }
    .komisi-card {
      background: var(--color-charcoal); border-top: 3px solid var(--color-crimson);
      padding: 1.75rem; transition: all var(--dur-normal) var(--ease-spring);
    }
    .komisi-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 40px rgba(139,26,26,0.25);
      border-top-color: var(--color-gold);
    }
    .komisi-number {
      font-family: var(--font-mono); font-size: 0.7rem; color: var(--color-gold);
      letter-spacing: 0.15em; text-transform: uppercase; margin-bottom: 0.5rem;
    }
    .komisi-title-card {
      font-family: var(--font-display); font-size: 1.15rem; font-weight: 700;
      color: var(--color-text-on-dark); margin-bottom: 0.75rem;
    }
    .komisi-desc {
      font-size: 0.9rem; color: rgba(245,240,232,0.65); line-height: 1.6; margin-bottom: 1rem;
    }
    .komisi-tags { display: flex; flex-wrap: wrap; gap: 0.5rem; }
    .tag {
      font-family: var(--font-mono); font-size: 0.6rem; color: rgba(245,240,232,0.5);
      background: rgba(201,168,76,0.1); border: 1px solid rgba(201,168,76,0.15);
      padding: 0.25rem 0.6rem; letter-spacing: 0.05em;
    }
    .komisi-divider { background: var(--color-gold) !important; }
    .komisi-label { color: var(--color-gold) !important; }

    @media (max-width: 1023px) { .komisi-grid { grid-template-columns: repeat(2, 1fr); } }
    @media (max-width: 767px) { .komisi-grid { grid-template-columns: 1fr; } }

    /* ============================================================
       OUTPUT CARDS
       ============================================================ */
    .output-grid {
      display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem;
    }
    .output-card {
      background: var(--color-paper); padding: 1.75rem;
      border-left: 3px solid var(--color-crimson);
      transition: all var(--dur-normal) var(--ease-spring);
    }
    .output-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 36px rgba(139,26,26,0.12);
    }
    .output-number {
      font-family: var(--font-mono); font-size: 2rem; font-weight: 700;
      color: var(--color-crimson); opacity: 0.12; line-height: 1; margin-bottom: 0.5rem;
    }
    .output-title {
      font-family: var(--font-display); font-size: 1.1rem; font-weight: 700;
      color: var(--color-crimson); margin-bottom: 0.5rem;
    }
    .output-desc { font-size: 0.9rem; color: var(--color-text-secondary); line-height: 1.6; }
    .output-divider { background: var(--color-crimson) !important; }

    @media (max-width: 767px) { .output-grid { grid-template-columns: 1fr; } }

    /* ============================================================
       REPRESENTASI
       ============================================================ */
    .rep-grid {
      display: grid; grid-template-columns: repeat(7, 1fr); gap: 1rem;
    }
    .rep-item {
      text-align: center; padding: 1.25rem 0.5rem;
      background: rgba(245,240,232,0.08); border: 1px solid rgba(245,240,232,0.1);
      transition: all var(--dur-normal) var(--ease-spring);
    }
    .rep-item:hover {
      background: rgba(245,240,232,0.15); transform: translateY(-4px);
      border-color: var(--color-gold);
    }
    .rep-icon { font-size: 1.75rem; margin-bottom: 0.5rem; }
    .rep-item span {
      font-family: var(--font-mono); font-size: 0.65rem; color: var(--color-text-on-dark);
      text-transform: uppercase; letter-spacing: 0.05em; line-height: 1.3;
    }
    .rep-divider { background: var(--color-gold) !important; }
    .rep-label { color: var(--color-gold) !important; }

    @media (max-width: 1023px) { .rep-grid { grid-template-columns: repeat(5, 1fr); } }
    @media (max-width: 767px) { .rep-grid { grid-template-columns: repeat(3, 1fr); } }

    /* ============================================================
       FESTIVAL CARDS
       ============================================================ */
    .festival-grid {
      display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.5rem;
    }
    .festival-card {
      background: var(--color-charcoal); padding: 2rem 1.5rem; text-align: center;
      border: 1px solid rgba(201,168,76,0.1);
      transition: all var(--dur-normal) var(--ease-spring);
    }
    .festival-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 16px 40px rgba(0,0,0,0.3);
      border-color: var(--color-gold);
    }
    .festival-icon { font-size: 2.5rem; margin-bottom: 1rem; }
    .festival-card-title {
      font-family: var(--font-display); font-size: 1.1rem; font-weight: 700;
      color: var(--color-text-on-dark); margin-bottom: 0.5rem;
    }
    .festival-card-desc { font-size: 0.85rem; color: rgba(245,240,232,0.6); line-height: 1.6; }
    .festival-divider { background: var(--color-gold) !important; }
    .festival-label { color: var(--color-gold) !important; }

    @media (max-width: 1023px) { .festival-grid { grid-template-columns: repeat(2, 1fr); } }
    @media (max-width: 767px) { .festival-grid { grid-template-columns: 1fr; } }

    /* ============================================================
       KONSER CARDS
       ============================================================ */
    .konser-grid {
      display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem;
    }
    .konser-card {
      background: var(--color-charcoal); padding: 1.75rem;
      border: 1px solid rgba(201,168,76,0.1);
      transition: all var(--dur-normal) var(--ease-spring);
    }
    .konser-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 36px rgba(0,0,0,0.3);
      border-color: var(--color-gold);
    }
    .konser-card-featured {
      border-color: var(--color-gold);
      background: linear-gradient(135deg, var(--color-charcoal) 0%, rgba(201,168,76,0.08) 100%);
    }
    .konser-card-order {
      font-family: var(--font-mono); font-size: 0.65rem; color: var(--color-gold);
      text-transform: uppercase; letter-spacing: 0.15em; margin-bottom: 0.5rem;
    }
    .konser-card-name {
      font-family: var(--font-display); font-size: 1.15rem; font-weight: 700;
      color: var(--color-text-on-dark); margin-bottom: 0.25rem;
    }
    .konser-card-genre { font-size: 0.8rem; color: rgba(245,240,232,0.5); }
    .konser-note {
      text-align: center; margin-top: 2rem; font-size: 0.8rem;
      color: rgba(245,240,232,0.4); font-style: italic;
    }
    .konser-divider { background: var(--color-gold) !important; }
    .konser-label { color: var(--color-gold) !important; }

    @media (max-width: 1023px) { .konser-grid { grid-template-columns: repeat(2, 1fr); } }
    @media (max-width: 767px) { .konser-grid { grid-template-columns: 1fr; } }

    /* ============================================================
       KEPANITIAAN
       ============================================================ */
    .panitia-stack { display: flex; flex-direction: column; gap: 2rem; margin-top: 1rem; }
    .panitia-level {
      padding: 2rem; border-left: 3px solid var(--color-crimson);
      background: var(--color-paper);
    }
    .panitia-level-1 { border-left-width: 5px; }
    .panitia-level-label {
      font-family: var(--font-mono); font-size: 0.7rem; color: var(--color-crimson);
      text-transform: uppercase; letter-spacing: 0.15em; margin-bottom: 1rem;
    }
    .panitia-names { display: flex; flex-wrap: wrap; gap: 0.75rem; }
    .panitia-name {
      font-size: 0.95rem; color: var(--color-text-primary); padding: 0.35rem 0;
    }
    .panitia-name.big {
      font-family: var(--font-display); font-size: 1.15rem; font-weight: 700;
      color: var(--color-crimson);
    }
    .panitia-name.dim { color: var(--color-text-muted); font-style: italic; }
    .panitia-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem; width: 100%; }
    .panitia-role { display: flex; flex-direction: column; gap: 0.25rem; }
    .role-title {
      font-family: var(--font-mono); font-size: 0.65rem; color: var(--color-text-muted);
      text-transform: uppercase; letter-spacing: 0.1em;
    }
    .role-name {
      font-family: var(--font-display); font-size: 1.1rem; font-weight: 700;
      color: var(--color-crimson);
    }
    .panitia-divider { background: var(--color-crimson) !important; }

    @media (max-width: 767px) {
      .panitia-grid { grid-template-columns: 1fr; }
      .panitia-names { flex-direction: column; gap: 0.25rem; }
    }

    /* ============================================================
       RUNDOWN
       ============================================================ */
    .rundown-divider { background: var(--color-crimson) !important; }
    .rundown-timeline { display: flex; flex-direction: column; gap: 0; margin-top: 1rem; }
    .rundown-item {
      display: flex; align-items: baseline; gap: 1.5rem; padding: 0.85rem 0;
      border-bottom: 1px solid var(--color-border);
    }
    .rundown-item:last-child { border-bottom: none; }
    .rundown-time {
      font-family: var(--font-mono); font-size: 0.8rem; color: var(--color-gold);
      font-weight: 700; white-space: nowrap; min-width: 120px;
    }
    .rundown-event { font-size: 0.95rem; color: var(--color-text-primary); }
    .rundown-item-highlight {
      background: rgba(139,26,26,0.06); padding: 0.85rem 1rem; margin: 0 -1rem;
      border-left: 3px solid var(--color-crimson); border-bottom: none;
    }
    .rundown-item-highlight .rundown-event { font-weight: 700; color: var(--color-crimson); }

    @media (max-width: 767px) {
      .rundown-item { flex-direction: column; gap: 0.25rem; }
      .rundown-time { min-width: auto; }
    }

    /* ============================================================
       REGISTRASI FORM
       ============================================================ */
    .registrasi-divider { background: var(--color-gold) !important; }
    .registrasi-label { color: var(--color-gold) !important; }
    .registrasi-quote {
      text-align: center; font-family: var(--font-display); font-style: italic;
      font-size: 1.25rem; color: var(--color-gold); margin: 1.5rem 0;
    }
    .registrasi-form {
      max-width: 560px; margin: 0 auto; background: var(--color-paper);
      padding: 2.5rem; border: 1px solid rgba(245,240,232,0.15);
    }
    .form-group { margin-bottom: 1.25rem; }
    .form-group label {
      display: block; font-family: var(--font-mono); font-size: 0.7rem;
      color: var(--color-text-primary); text-transform: uppercase;
      letter-spacing: 0.1em; margin-bottom: 0.5rem;
    }
    .form-group label .required { color: var(--color-crimson); }
    .form-group input,
    .form-group select,
    .form-group textarea {
      width: 100%; padding: 0.75rem 1rem; background: var(--color-ivory);
      border: 1px solid var(--color-border); font-family: var(--font-body);
      font-size: 1rem; color: var(--color-text-primary);
      transition: border-color var(--dur-fast);
    }
    .form-group input:focus,
    .form-group select:focus,
    .form-group textarea:focus {
      outline: none; border-color: var(--color-crimson);
      box-shadow: 0 0 0 3px rgba(139,26,26,0.1);
    }
    .form-group textarea { resize: vertical; min-height: 100px; }
    .btn-submit {
      width: 100%; padding: 1rem; background: var(--color-gold);
      color: var(--color-deep-black); font-family: var(--font-mono);
      font-size: 0.8rem; font-weight: 700; letter-spacing: 0.15em;
      text-transform: uppercase; border: none; cursor: pointer;
      transition: all var(--dur-normal) var(--ease-out);
    }
    .btn-submit:hover {
      background: var(--color-gold-light); transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(201,168,76,0.3);
    }
    .form-thanks {
      max-width: 560px; margin: 0 auto; text-align: center; padding: 3rem 2rem;
      background: var(--color-paper);
    }
    .thanks-icon {
      font-size: 3rem; color: #2ecc71; margin-bottom: 1rem;
    }
    .thanks-title {
      font-family: var(--font-display); font-size: 1.5rem; font-weight: 700;
      color: var(--color-crimson); margin-bottom: 0.5rem;
    }
    .thanks-desc { color: var(--color-text-secondary); }
    .registrasi-alt {
      text-align: center; margin-top: 2rem; font-size: 0.9rem;
      color: rgba(245,240,232,0.7);
    }
    .registrasi-alt strong { color: var(--color-gold); }

    @media (max-width: 767px) {
      .registrasi-form { padding: 1.5rem; }
    }

    /* ============================================================
       FOOTER
       ============================================================ */
    .footer {
      background: var(--color-deep-black); color: var(--color-text-on-dark);
      padding: var(--section-pad-y) var(--section-pad-x);
      position: relative;
    }
    .footer::before {
      content: ''; position: absolute; inset: 0; pointer-events: none;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.02'/%3E%3C/svg%3E");
      mix-blend-mode: overlay; z-index: 0;
    }
    .footer > * { position: relative; z-index: 1; }
    .footer-grid {
      display: grid; grid-template-columns: 2fr 1fr 1fr; gap: 3rem;
      max-width: var(--container); margin: 0 auto;
    }
    .footer-logo {
      font-family: var(--font-mono); font-size: 1.25rem; font-weight: 700;
      color: var(--color-gold); letter-spacing: 0.1em; margin-bottom: 0.75rem;
    }
    .footer-desc { font-size: 0.9rem; color: rgba(245,240,232,0.7); margin-bottom: 0.5rem; }
    .footer-info { font-family: var(--font-mono); font-size: 0.7rem; color: rgba(245,240,232,0.5); }
    .footer-heading {
      font-family: var(--font-mono); font-size: 0.7rem; color: var(--color-gold);
      text-transform: uppercase; letter-spacing: 0.15em; margin-bottom: 1rem;
    }
    .footer-nav ul, .footer-contact ul { list-style: none; padding: 0; }
    .footer-nav li, .footer-contact li { margin-bottom: 0.5rem; }
    .footer-nav a {
      color: rgba(245,240,232,0.7); text-decoration: none; font-size: 0.9rem;
      transition: color var(--dur-fast);
    }
    .footer-nav a:hover { color: var(--color-gold); }
    .footer-contact li { font-size: 0.9rem; color: rgba(245,240,232,0.6); }
    .footer-social-heading { margin-top: 1.5rem; }
    .footer-divider {
      max-width: var(--container); margin: 3rem auto;
      height: 1px; background: rgba(201,168,76,0.2);
    }
    .footer-quote {
      text-align: center; font-family: var(--font-display); font-style: italic;
      font-size: 1.1rem; color: var(--color-gold); margin: 1.5rem 0;
    }
    .footer-copy {
      text-align: center; font-size: 0.8rem; color: rgba(245,240,232,0.5);
    }
    .footer-note {
      text-align: center; font-size: 0.7rem; color: rgba(245,240,232,0.3); margin-top: 0.5rem;
    }

    @media (max-width: 767px) {
      .footer-grid { grid-template-columns: 1fr; gap: 2rem; }
    }
  </style>
</head>
<body>
  <div class="scroll-progress" id="scrollProgress"></div>
  <slot />
  <noscript>
  <style is:global>
      [data-animate] { opacity: 1 !important; transform: none !important; }
      .scroll-progress { display: none; }
    </style>
  </noscript>
  <script>
    // Scroll progress
    window.addEventListener('scroll', () => {
      const scrollTop = window.scrollY;
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      document.getElementById('scrollProgress').style.width = (scrollTop / docHeight * 100) + '%';
    }, { passive: true });

    // Navbar scroll effect
    const navbar = document.querySelector('.navbar');
    window.addEventListener('scroll', () => {
      if (navbar) navbar.classList.toggle('scrolled', window.scrollY > 80);
    }, { passive: true });

    // Intersection Observer
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });

    document.querySelectorAll('[data-animate]').forEach(el => observer.observe(el));

    // Mobile nav toggle
    const navToggle = document.querySelector('.nav-toggle');
    const navLinks = document.querySelector('.nav-links');
    if (navToggle && navLinks) {
      navToggle.addEventListener('click', () => navLinks.classList.toggle('open'));
      navLinks.querySelectorAll('a').forEach(a => {
        a.addEventListener('click', () => navLinks.classList.remove('open'));
      });
    }
  </script>
</body>
</html>
